apiVersion: v1
kind: Service
metadata:
  name: addressbook-service
spec:
  type: NodePort
  selector:
    app: second-end
  ports:
    - protocol: TCP
      port: 8082
      targetPort: http-web-app-01
      